'use strict';

// Declare app level module which depends on views, and components
angular.module('app', [
    'app.core',
    'app.layout',
    'app.vault',
    'app.account',
    'app.accountService',
    'app.widgets',
    "app.vaultService"
]);
