'use strict';

// Declare app level module which depends on views, and components
angular.module('app', [
    'app.core',
    'app.layout',
    'app.widgets',
    'app.vault',
    'app.account',
    'app.auth',
    'app.services'
]);
